# referenced from https://github.com/AUTOMATIC1111/stable-diffusion-webui/blob/82a973c04367123ae98bd9abdf80d9eda9b910e2/.github/workflows/on_pull_request.yaml
name: Linter

on:
  - push
  - pull_request

jobs:
  lint-python:
    name: ruff & pydoclint
    runs-on: ubuntu-latest
    # ---don't need the if, just check every time---
    # if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v8.1.0
      - name: Run Ruff
        run: uv run --only-group dev ruff check --output-format=github
      - name: Run Pydoclint
        run: uv run --only-group dev pydoclint .
